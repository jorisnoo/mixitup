// Vitest jsdom environment setup
// The jsdom environment is configured in vitest.config.ts
// This file provides any additional setup needed for the test environment.

// Ensure global document and window are available (provided by jsdom env)
